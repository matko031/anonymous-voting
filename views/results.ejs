<%- include('includes/header') %>

    <% results.forEach( (position) => { %>

        <b> Position: </b> <%= position.position %> 
        <br>
        <b> People who voted: </b>
        <% position.voted.forEach((vote) => { %>
            <%=  vote  %>, 
        <% });%>
        <br>
        <b> Votes </b>
        <br>
        <% position.votes.forEach( (vote_number) => { %>
            <b> <%=  vote_number.name  %>: </b> <%= vote_number.number %> 
        <% });%>
        <br>
        <br>

    <% });%>
<%- include('includes/header') %>
