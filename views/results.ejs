<%- include('includes/header') %>

    <% Object.keys(results).forEach( (question) => { %>

        <b>Question: </b>
        <br>
        <%= question %>
        <br>
        <% Object.keys(results[question]).forEach( (answer) => { %>
            <%= answer %>: <%= results[question][answer] %>
            <br>

        <%});%>
        <br>
        <br>
    <% });%>
<%- include('includes/header') %>
